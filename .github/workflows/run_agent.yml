name: Super-Agent Pulse

on:
  workflow_dispatch:

jobs:
  run-alpha:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'

      - name: Install 2026 Libraries
        run: |
          pip install cosmpy uagents

      - name: Run Alpha Manager
        env:
          AGENT_SEED: ${{ secrets.AGENT_SEED }}
          AGENT_SEED_GAS: ${{ secrets.AGENT_SEED_GAS }}
          AGENT_SEED_AUDIT: ${{ secrets.AGENT_SEED_AUDIT }}
        run: python main.py
        
